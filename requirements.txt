# ---- Core numerics / tabular ----
numpy>=1.26,<2.2
pandas>=2.1,<2.3
pyarrow>=15.0
scikit-learn>=1.4

# ---- Deep learning (CPU wheels by default; install CUDA build separately if GPU) ----
torch>=2.0

# ---- Config management ----
omegaconf>=2.3
hydra-core>=1.3
pyyaml>=6.0

# ---- Explainability ----
shap>=0.44

# ---- Demo / serving ----
streamlit>=1.30
fastapi>=0.110
uvicorn[standard]>=0.27

# ---- Plotting ----
matplotlib>=3.8
seaborn>=0.13

# ---- Packet / PCAP handling ----
pyshark>=0.6          # TShark wrapper (requires system `tshark` on PATH)
scapy>=2.5

# ---- Utilities ----
tqdm>=4.66

# ---- Notebooks ----
jupyter>=1.0
ipykernel>=6.29

# ---- Testing ----
pytest>=8.0
pytest-cov>=4.1
plotly>=5.20
